@page "/"
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        // Portal SuperAdmin: la raiz siempre redirige al login. El dashboard
        // valida sesion via SuperAdminSession y redirige a /login si no hay sesion.
        Nav.NavigateTo("/login", replace: true);
    }
}
