<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Version VISIBLE de la plataforma (semver manual): bumpear en CADA deploy (0.0.1 -> 0.0.2 -> ...).
         Se muestra como "v0.0.1" en el login y el sidebar (PlatformVersion.Label). -->
    <Version>0.0.50</Version>
    <!-- Sella la fecha/hora del build (UTC) en el ensamblado para la trazabilidad exacta (tooltip). -->
    <BuildTimestampUtc>$([System.DateTime]::UtcNow.ToString("yyyy-MM-dd HH:mm"))</BuildTimestampUtc>
  </PropertyGroup>

  <ItemGroup>
    <AssemblyMetadata Include="BuildTimestampUtc" Value="$(BuildTimestampUtc)" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Propia.Web.Client\Propia.Web.Client.csproj" />
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.Server" Version="9.0.16" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Propia.Shared\Propia.Shared.csproj" />
    <ProjectReference Include="..\Propia.Application\Propia.Application.csproj" />
    <!-- Unificacion API+Web: la Web hostea tambien controllers/hubs/mcp/middleware de la API.
         Arrastra Propia.Infrastructure y los paquetes (JwtBearer, MCP, Serilog, Prometheus, HealthChecks) transitivamente. -->
    <ProjectReference Include="..\Propia.Api\Propia.Api.csproj" />
  </ItemGroup>

</Project>
