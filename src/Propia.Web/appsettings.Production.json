{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "PropiaApi": {
    "_comment": "Host unificado: la API vive en el MISMO contenedor (escucha en :8080). Loopback a si mismo.",
    "BaseUrl": "http://localhost:8080"
  },
  "App": {
    "_comment": "URL publica del app (para links en correos: invitaciones, etc). Debe ser la del dominio real.",
    "PublicBaseUrl": "https://app.propia-ad.com"
  }
}
