@page "/mascotas"
@rendermode @(new InteractiveServerRenderMode(prerender: false))

@* Ruta suelta del modulo Mascotas: wrapper fino sobre MascotasPanel (reutilizable en el hub). *@

<MascotasPanel />
