@page "/vehiculos"
@rendermode @(new InteractiveServerRenderMode(prerender: false))

@* Ruta suelta del modulo Vehiculos: wrapper fino sobre VehiculosPanel (reutilizable en el hub). *@

<VehiculosPanel />
