@page "/distribucion"
@rendermode @(new InteractiveServerRenderMode(prerender: false))

@* Ruta suelta del modulo Unidades Privadas: wrapper fino sobre DistribucionPanel (reutilizable en el hub). *@

<DistribucionPanel />
