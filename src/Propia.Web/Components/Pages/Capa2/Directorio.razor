@page "/directorio"
@rendermode @(new InteractiveServerRenderMode(prerender: false))

@* Ruta suelta del modulo Directorio: wrapper fino sobre DirectorioPanel (reutilizable en el hub). *@

<DirectorioPanel />
