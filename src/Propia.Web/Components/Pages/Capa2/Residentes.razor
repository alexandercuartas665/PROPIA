@page "/residentes"
@rendermode @(new InteractiveServerRenderMode(prerender: false))

@* Ruta suelta del modulo Residentes: wrapper fino que fija el render mode y renderiza el panel
   reutilizable. El mismo ResidentesPanel se embebe (Embedded="true") en el hub de Configuracion. *@

<ResidentesPanel />
