@page "/usuarios"
@rendermode @(new InteractiveServerRenderMode(prerender: false))

@* Ruta suelta del modulo Usuarios y Roles: wrapper fino sobre UsuariosPanel (reutilizable en el hub). *@

<UsuariosPanel />
