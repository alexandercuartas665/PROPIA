@page "/"
@inject NavigationManager Nav

@* Home publica: redirige al login. Ahi viven los dos caminos de entrada: "Iniciar sesion"
   y "Crear cuenta nueva" (registro self-service que arranca el wizard 2.1). La landing
   comercial real (si llega) se hara en un sitio aparte. *@

<PageTitle>PROPIA - Plataforma de Copropiedades</PageTitle>

<div class="text-center py-5"><div class="spinner-border" style="color: var(--propia-primary);"></div></div>

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/login", forceLoad: false);
    }
}
